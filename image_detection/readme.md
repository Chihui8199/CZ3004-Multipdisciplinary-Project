## Source Code Information for Algo Team  
1. Ignore all code except img_rec.py
2. 2 functions to be used is detect() and stitch()
3. object_name ,object_id, dist, angle = detect(), detect() function returns 4 values
4. stitch() function combines images in runs/detect/exp and save in result, to be used at the end of run

## Directory Information  
1. Raw Image taken and stored in data/images
2. If image is detected, saved image with bounding box is stored in runs/detect/exp
3. Stitched images is stored in result
